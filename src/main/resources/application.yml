spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pluma?serverTimezone=Asia/Seoul
    username: ${db_id}
    password: ${db_pw}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret : YourSuperSecretKeyForJWTSigning1234567890!


server:
  port: 7080
